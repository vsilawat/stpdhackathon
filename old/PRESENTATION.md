# Round 1 — 3-minute presentation

Target: under 3:00. The script is **411 spoken words** — verified at
**2:50** at a measured 145 wpm, **2:39** at a normal 155 wpm. That leaves
10–20s of buffer for pauses and slide transitions.

(An earlier draft ran 552 words / 3:34 and had to be cut. If you edit the
script, re-check the count — 465 words is the hard ceiling at 155 wpm.)

Rubric context this is written against:
- Round 1 requires the video + technical submission + a brief methods description.
- Round 2 scores originality, effectiveness/efficiency, documentation, generalizability, and **scientific rigor** ("reverse engineering or hacky solutions will be penalized").
- Section 4 requires **disclosing LLM use**. Covered on the closing slide.

---

## Slide outline

| # | Time | Slide | Visual |
|---|---|---|---|
| 1 | 0:00–0:12 | The problem | One part render (`isometric_shaded.png`) → arrow → a short operation list |
| 2 | 0:12–0:40 | The key idea | Two-column: "learn the mapping" ✗ vs "reconstruct the rulebook" ✓ |
| 3 | 0:40–1:25 | The discovery | **The money slide.** Four pocket diagrams showing 1 / 2 / 3 / 4 corner fillets, each labelled with the feature it implies |
| 4 | 1:25–1:55 | Results | Rubric table + "20,000/20,000 files valid" |
| 5 | 1:55–2:35 | Measured limits | Two panels: the in-process-state diagram, and the 82%/9% volume split |
| 6 | 2:35–2:55 | What we'd take forward + disclosure | One line of lesson, then the LLM disclosure block |

**Slide 3 is the one to spend design effort on.** It carries the originality
score. Draw the four cases as simple top-down outlines with the fillets
highlighted in a contrasting colour.

---

## Script

Spoken lines are prefixed `>`. The disclosure block on Slide 6 is **on-screen
only** — not read aloud — and is excluded from the word count.

### Slide 1 — The problem `[0:00–0:12]`

> Given a CAD model of a metal part, predict how to machine it: which
> operations, in what order, and with which tools. Our data — ten thousand
> parts, ninety-one thousand machining operations, generated in Siemens NX.

### Slide 2 — The key idea `[0:12–0:40]`

> One observation shaped everything. These labels weren't written by a person.
> They came from NX's deterministic knowledge base. The target isn't fuzzy
> human judgment — it's a rulebook.
>
> So we reconstructed the rules we could verify exactly, and learned only what
> we couldn't. Most stages contain no machine learning, and the pipeline has no
> third-party dependencies — including our own STEP reader. Ten thousand parts
> in twenty seconds.

### Slide 3 — The discovery `[0:40–1:25]`

> Here's the idea we're proudest of.
>
> A milling cutter is round. It cannot cut a sharp internal corner — it always
> leaves a fillet of exactly its own radius. So the corners it leaves behind
> are a fingerprint.
>
> Count them, and you know the feature. One corner: a notch in the block's
> edge. Two: a slot open at one end. Three: an open pocket. Four: a fully
> enclosed pocket. Each needs a completely different operation.
>
> We were already measuring those corners. We just weren't counting them. That
> one number took our pocket accuracy from forty-six percent to eighty-five.

### Slide 4 — Results `[1:25–1:55]`

> Against the organizers' rubric: Easy scores fourteen out of twenty — F1 of
> 0.93, edit distance 0.204. Tool selection, about ten out of twenty. And when
> we name the operation correctly, the tool diameter is exact — median relative
> error of zero. All twenty thousand submission files pass the organizers' own
> validator.

### Slide 5 — Measured limits `[1:55–2:35]`

> We also measured where we can't go.
>
> Tool selection is capped. NX picks a drill from how wide the hole already is
> partway through the job. That in-process state doesn't exist in the finished
> CAD model we're given. We measured the residual uncertainty at 0.93 bits —
> and our attempts to beat it made results worse.
>
> We also chose not to attempt the mesh and G-code tracks. Eighty-two percent
> of all material removed is pockets and slots; drilling is nine. Those tracks
> score zero below a threshold, so a drilling-only submission would have scored
> zero. We measured that before deciding, not after.

### Slide 6 — What we'd take forward `[2:35–2:55]`

> One caution against our own judgment. We first wrote off operation ordering
> as a coin flip. Testing it properly made it one of our largest gains —
> eighty-nine percent accurate, from the depth of the shallowest hole.
>
> The lesson: when the data comes from a rule engine, the wins are structure
> that is already there.

**On-screen only, Slide 6 (do not read aloud):**

    Claude Opus 5 (Anthropic) was used to write code and documentation.
    All submitted outputs were generated by our own deterministic pipeline;
    no model produced the easy, medium, or hard outputs directly.

## Delivery notes

- **Pace.** 155 wpm is brisk but natural. Rehearse Slide 3 slowest — it carries
  the originality score and the four-case list needs beats between items.
- **The strongest 12 seconds** are "We were already measuring those corners. We
  simply weren't counting them." Pause before it.
- **Say a number, show a number.** Every figure spoken should be on screen.
- **Don't apologise for the skipped tracks.** Frame them as a measured decision
  — that reads as engineering judgment, which Round 2 scores. The 82%/9% split
  is the evidence; lead with it.
- **If you overrun**, cut "Here's the idea we're proudest of" (Slide 3) and the
  second half of Slide 4 — the validator line — first. Protect the four-corner
  list; it is the whole originality argument.
- **If you have spare time**, the best thing to add back is chamfers: on a
  2.5-axis part a chamfer is the only non-axis-aligned flat face, and that test
  is correct on 1500/1500 parts. It is a second, independent example of the same
  physical-reasoning idea (~25 words).

## Rigor framing (important)

Rubric 3.2 penalizes "reverse engineering or hacky solutions." Our approach
reconstructs NX's rules, so state the defence plainly — one line on Slide 2 or
in the written methods description:

> Rules are learned from the training labels only. At inference the pipeline
> reads **nothing but the CAD file** — no toolpaths, no operation cards, no
> ground-truth metadata. This is ordinary supervised learning; the rules are
> the model.

This is true of the code as written (`predict()` parses only the `.stp`), and
it is worth saying explicitly before a judge wonders.

## Written methods description

The rubric asks for a brief methods description alongside the video. `README.md`
already covers approach, confirmed-exact facts with evidence, results, known
data issues, and limitations. Suggested additions if you want it standalone:
a one-paragraph literature note (MFCAD / MFCAD++ / BrepMFR do feature
*recognition*; this problem is process *planning*, which is a harder target),
and a generalizability paragraph — the parts are stock blocks minus features
with planar and cylindrical faces only, so the fillet-counting rule would need
extending for freeform geometry.
